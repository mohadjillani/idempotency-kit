# Changesets

This folder holds pending release notes. Run `npx changeset` after changing a published package, pick the bump (patch, minor, major) per package, and describe the change from a user's point of view. The release workflow turns pending changesets into a "Version Packages" pull request; merging it publishes to npm with provenance.

Full documentation: https://github.com/changesets/changesets
